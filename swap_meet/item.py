# need to import Vendor class?

class Item:
    # When we initialize an instance of Item, we can optionally pass in a string with the keyword argument category
    def __init__(self, category=""):
        self.category = category
